pipeline{
    agent any
	stages{
		stage(Execute){
			steps {
				script {
		ANSIBLE_PRIVATE_KEY="1234"
	}
				// sh 'ansible-playbook -i host.inv --private-key=$ANSIBLE_PRIVATE_KEY git.yml'
				echo "${ANSIBLE_PRIVATE_KEY}"
			}
		}
	}
}
