pipeline{
    agent any
    environment {
        AWS_ACCESS_KEY_ID     = credentials('private-key1')
        // AWS_SECRET_ACCESS_KEY = credentials('jenkins-aws-secret-access-key')
    }
	stages{
		stage(Execute){
			steps {
				   sh 'ansible-playbook -i host.inv --private-key=$AWS_ACCESS_KEY_ID git.yml'
				// echo "${AWS_ACCESS_KEY_ID}"
			}
		}
	}
}
